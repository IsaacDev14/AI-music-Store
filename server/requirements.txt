# Core FastAPI framework
fastapi==0.111.1
uvicorn[standard]==0.23.2

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.7
alembic==1.12.0

# Data validation & config
pydantic==2.7.2
python-dotenv==1.0.0

# Optional: async DB support if needed
databases==0.9.0

# HTTP requests (for calling external APIs like Chordify, Hugging Face, Gemini)
httpx==0.24.1
requests==2.32.0

# Optional AI / ML dependencies (Hugging Face)
transformers==4.44.2
torch==2.2.0  # or cpu-only: torch==2.2.0+cpu depending on your environment

# Testing
pytest==8.3.2
pytest-asyncio==0.22.0
